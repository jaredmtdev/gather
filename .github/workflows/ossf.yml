on:
  push:
    #branches:
    #  - main
  schedule:
    - cron: '0 9 * * 1'  # weekly, Mondays 09:00 UTC

permissions:
  contents: read
  security-events: write
  id-token: write

jobs:
  scorecard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ossf/scorecard-action@v5
        with:
          publish_results: true
